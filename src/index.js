import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

// import {createBrowser} from 'reacr-router-dom'

const root = ReactDOM.createRoot(document.getElementById('root'));

// const routes = createBroweser
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

